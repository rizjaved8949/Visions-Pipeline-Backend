Guard monitoring integration verification

Verified after integrating into the uploaded Visions-Pipeline-Backend structure:
- Existing Guard_monotoring wrapper structure retained.
- Existing launcher app.py retained; no second FastAPI app created.
- Guard APIRouter registered in Attendance/server.py.
- One root .env retained and guard paths point inside Guard_monotoring.
- One root requirements.txt retained, de-duplicated, with unpinned package names.
- Fine-tuned RF-DETR checkpoint loading wired through RFDETR.from_checkpoint().
- Checkpoint filename and trained class label are intentionally not guessed.
- Python compileall passed.
- Guard unit/failure-isolation tests passed: 8 passed.
- Nested CLI --help passed from repository root.
- Guard API import remained lazy: heavy CV model packages are not loaded by importing the router.

Not verified because the required artifact was not uploaded:
- Accuracy/inference of the user's actual fine-tuned RF-DETR checkpoint.
- Exact trained class label inside that checkpoint.
- Real CCTV accuracy on the user's local videos.

Those require copying the user's downloaded checkpoint into Guard_monotoring/weights/guard/
and setting GUARD_RFDETR_WEIGHTS in the root .env.
